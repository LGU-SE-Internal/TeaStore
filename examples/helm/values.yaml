clientside_loadbalancer: true # Deploys as Statefulset which enables communication to every instance individually. A kubernetes service is used otherwise

# Global image configuration
# Services will use these values unless explicitly overridden
global:
  image:
    # Container registry (e.g., "10.10.10.240/library", "docker.io/descartesresearch")
    registry: "10.10.10.240/library"
    # Default image tag for all services
    tag: "aa0211b7"
    # Default pull policy
    pullPolicy: IfNotPresent

webui:
  enabled: true
  replicaCount: 1
  image:
    # Leave empty to use global.image.registry
    registry: ""
    name: "teastore-webui"
    pullPolicy: ""
    # Leave empty to use global.image.tag
    tag: ""
  imagePullSecrets: []
  podAnnotations: {}
  podSecurityContext: {}
  securityContext: {}
  service:
    type: NodePort
    port: 80
    annotations: {}
  resources: {}
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 100
    targetCPUUtilizationPercentage: 80
    # targetMemoryUtilizationPercentage: 80
  nodeSelector: {}
  tolerations: []
  affinity: {}
  svc_name: teastore-webui
registry:
  enabled: true
  image:
    # Leave empty to use global.image.registry
    registry: ""
    name: "teastore-registry"
    pullPolicy: ""
    # Leave empty to use global.image.tag
    tag: ""
  imagePullSecrets: []
  podAnnotations: {}
  podSecurityContext: {}
  securityContext: {}
  service:
    port: 8080
    annotations: {}
  resources: {}
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 100
    targetCPUUtilizationPercentage: 80
    # targetMemoryUtilizationPercentage: 80
  nodeSelector: {}
  tolerations: []
  affinity: {}
  svc_name: teastore-registry
db:
  enabled: true
  image:
    # Leave empty to use global.image.registry
    registry: ""
    name: "teastore-db"
    pullPolicy: ""
    # Leave empty to use global.image.tag
    tag: ""
  imagePullSecrets: []
  podAnnotations: {}
  podSecurityContext: {}
  securityContext: {}
  service:
    port: 3306
    annotations: {}
  resources: {}
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 100
    targetCPUUtilizationPercentage: 80
    # targetMemoryUtilizationPercentage: 80
  nodeSelector: {}
  tolerations: []
  affinity: {}
  svc_name: teastore-db
auth:
  enabled: true
  replicaCount: 1
  image:
    # Leave empty to use global.image.registry
    registry: ""
    name: "teastore-auth"
    pullPolicy: ""
    # Leave empty to use global.image.tag
    tag: ""
  imagePullSecrets: []
  podAnnotations: {}
  podSecurityContext: {}
  securityContext: {}
  service:
    type: NodePort
    port: 8080
    annotations: {}
  resources: {}
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 100
    targetCPUUtilizationPercentage: 80
    # targetMemoryUtilizationPercentage: 80
  nodeSelector: {}
  tolerations: []
  affinity: {}
  svc_name: teastore-auth
image:
  enabled: true
  replicaCount: 1
  image:
    # Leave empty to use global.image.registry
    registry: ""
    name: "teastore-image"
    pullPolicy: ""
    # Leave empty to use global.image.tag
    tag: ""
  imagePullSecrets: []
  podAnnotations: {}
  podSecurityContext: {}
  securityContext: {}
  service:
    type: NodePort
    port: 8080
    annotations: {}
  resources: {}
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 100
    targetCPUUtilizationPercentage: 80
    # targetMemoryUtilizationPercentage: 80
  nodeSelector: {}
  tolerations: []
  affinity: {}
  svc_name: teastore-image
recommender:
  enabled: true
  replicaCount: 1
  image:
    # Leave empty to use global.image.registry
    registry: ""
    name: "teastore-recommender"
    pullPolicy: ""
    # Leave empty to use global.image.tag
    tag: ""
  imagePullSecrets: []
  podAnnotations: {}
  podSecurityContext: {}
  securityContext: {}
  service:
    type: NodePort
    port: 8080
    annotations: {}
  resources: {}
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 100
    targetCPUUtilizationPercentage: 80
    # targetMemoryUtilizationPercentage: 80
  nodeSelector: {}
  tolerations: []
  affinity: {}
  svc_name: teastore-recommender
persistence:
  enabled: true
  replicaCount: 1
  image:
    # Leave empty to use global.image.registry
    registry: ""
    name: "teastore-persistence"
    pullPolicy: ""
    # Leave empty to use global.image.tag
    tag: ""
  imagePullSecrets: []
  podAnnotations: {}
  podSecurityContext: {}
  securityContext: {}
  service:
    type: NodePort
    port: 8080
    annotations: {}
  resources: {}
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 100
    targetCPUUtilizationPercentage: 80
    # targetMemoryUtilizationPercentage: 80
  nodeSelector: {}
  tolerations: []
  affinity: {}
  svc_name: teastore-persistence

# OpenTelemetry Instrumentation settings
opentelemetry:
  enabled: true
  # Language type for auto-instrumentation (drives the
  # `instrumentation.opentelemetry.io/inject-<language>` pod annotation).
  # Supported: java, python, nodejs, dotnet, go, ruby, php
  language: java
  # Name of the OpenTelemetry `Instrumentation` custom resource the operator
  # should inject. Used as the right-hand side of the inject annotation:
  #   instrumentation.opentelemetry.io/inject-<language>: "<monitoringNamespace>/<instrumentationName>"
  # Override when your Instrumentation CR has a different name.
  instrumentationName: "opentelemetry-kube-stack"
  # Namespace where the `Instrumentation` CR above lives. Override when the
  # operator is configured with its CR outside the conventional `monitoring`
  # namespace (e.g. `otel`, `observability`). The operator webhook silently
  # skips injection if <namespace>/<name> does not resolve.
  monitoringNamespace: "monitoring"
  # OTLP exporter endpoint for traces and logs
  # This should point to your OTEL Collector service
  otlpEndpoint: "http://opentelemetry-kube-stack-deployment-collector.monitoring.svc.cluster.local:4317"
  # OTLP protocol: grpc or http/protobuf
  otlpProtocol: "grpc"
  # Enable logs export via OTLP
  logsExporterEnabled: true
  # Enable traces export via OTLP
  tracesExporterEnabled: true
  # Enable metrics export via OTLP
  metricsExporterEnabled: true
  # Sampling ratio (1.0 = 100% sampling)
  samplingRatio: "1.0"

# HTTP Load Generator settings
httploadgen:
  enabled: false
  replicaCount: 1
  image:
    # Leave empty to use global.image.registry
    registry: ""
    name: "teastore-httploadgen"
    pullPolicy: Always
    # Leave empty to use global.image.tag
    tag: ""
  service:
    type: ClusterIP
    port: 24226
  resources:
    limits:
      cpu: 2000m
      memory: 2Gi
    requests:
      cpu: 500m
      memory: 512Mi
  nodeSelector: {}

# HTTP Load Director settings
httploaddirector:
  enabled: false
  restartPolicy: Never
  image:
    # Leave empty to use global.image.registry
    registry: ""
    name: "teastore-httploaddirector"
    pullPolicy: Always
    # Leave empty to use global.image.tag
    tag: ""
  # Load profile to use (file path in container)
  loadProfile: "/director/profiles/increasingLowIntensity.csv"
  # Lua script to use (file path in container)
  luaScript: "/director/scripts/teastore_browse.lua"
  # Output file path
  outputFile: "/director/results/output.csv"
  # Number of load generation threads
  numThreads: 10
  # Docker monitoring configuration
  dockerMonitor:
    enabled: false
    # Comma-separated values: <IP>:<PORT>:<CONTAINER_ID>,<IP>:<PORT>:<CONTAINER_ID>
    targets: ""
  resources:
    limits:
      cpu: 1000m
      memory: 1Gi
    requests:
      cpu: 250m
      memory: 256Mi
  nodeSelector: {}

# JMeter Load Testing settings
jmeter:
  enabled: true
  backoffLimit: 3
  restartPolicy: OnFailure
  image:
    # Leave empty to use global.image.registry
    registry: ""
    name: "teastore-jmeter"
    pullPolicy: Always
    # Leave empty to use global.image.tag
    tag: ""
  # Image used by the `wait-for-registry` init container. Override on
  # air-gapped / mirrored-registry clusters that cannot pull from docker.io.
  # Leaving `registry` empty preserves the historical default of pulling
  # `busybox:1.36` from docker.io (no behavior change for existing users).
  # Example for a mirrored registry:
  #   --set jmeter.waitForRegistryImage.registry=pair-cn-shanghai.cr.volces.com/opspai
  # produces: pair-cn-shanghai.cr.volces.com/opspai/busybox:1.36
  waitForRegistryImage:
    registry: ""
    repository: "busybox"
    tag: "1.36"
    pullPolicy: ""
  # Target WebUI service
  targetHost: "teastore-webui"
  targetPort: "8080"
  # Resource limits to prevent OOM and cluster resource exhaustion
  resources:
    limits:
      cpu: 2000m
      memory: 2Gi
    requests:
      cpu: 500m
      memory: 1Gi
  # Test configuration (add these if not present)
  # Defaults capped to keep span-emission rate in line with other DSB-style
  # benchmarks (TT, sockshop, otel-demo). Earlier defaults of numUsers=10 +
  # duration=300 produced ~22x the spans/sec of comparable benchmarks and
  # could overload shared OTel collector / ClickHouse pipelines when several
  # teastore namespaces ran in parallel. Override via --set jmeter.numUsers=10
  # --set jmeter.duration=300 if your collector pipeline can absorb it.
  # See: https://github.com/OperationsPAI/aegis/issues/216
  numUsers: 2
  rampUp: 60
  duration: 120
  testPlan: "teastore_browse.jmx"
  nodeSelector: {}
